# Contraseña para acceder al tablero
APP_PASSWORD=cambia-esta-contrasena

# Secreto para firmar las sesiones — genera uno largo y aleatorio:
#   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=

# Puerto del servidor
PORT=3001

# Ruta del archivo SQLite (en Docker se monta en un volumen)
DB_PATH=./data/linkboard.db

# true cuando se sirve detrás de HTTPS (cookies con flag Secure)
COOKIE_SECURE=false
